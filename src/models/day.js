export default class DayModel {
  constructor(params) {
    this.string = params.string;
    this.day = params.day;
    this.month = params.month;
    this.monthText = params.monthText;
    this.year = params.year;
  }
}
